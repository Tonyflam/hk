{
  "name": "@nexus-402/contracts",
  "version": "1.0.0",
  "description": "Smart contracts for NEXUS-402 protocol on Cronos EVM",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network cronos-testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network cronos-mainnet",
    "verify": "hardhat verify --network cronos-testnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.1",
    "hardhat": "^2.19.4",
    "dotenv": "^16.3.1"
  }
}
